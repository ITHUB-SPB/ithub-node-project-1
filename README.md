# Сопровождение курса "Основы Node.JS"

## (КТ10 + КТ11) Шаблоны: верстка и наполнение данными. Обработка форм и асинхронных запросов.

_Максимальный балл - 10 (КТ10) + 8 (КТ11) = 18._

[Макет](https://www.figma.com/design/2MK0srHXelMlZjnjQEuErO/Meeting-Room--Community---Copy-?node-id=0-1&p=f&t=7mFhe0cDNUKJFItV-0)

### Требования и критерии оценивания

#### Критерии КТ#11

1. Разработать (сверстать) шаблон index по макету (экран Meeting Room Screen) - до 4 баллов,
2. Разработать (сверстать) шаблон detail по макету (экран Room Detail Screen) - до 2 баллов,
3. Разработать (сверстать) шаблон booking по макету (экран Booking Screen) - до 2 баллов

#### Критерии КТ#10

1. Добавить данные о наличии в комнатах плазмы, доски, беспроводной сети (1/3 балла)

_1 балл за дополнение существующей таблицы с игнорированием правил нормализации_, 

_3 балла за реализацию через создание отдельной таблицы с функциональными возможностями и создание промежуточной таблицы для связи многие-ко-многим_

2. Добавить информацию о вместимости комнаты (1 балл)

3. Получать информацию о комнатах на главной странице (1 балл). Получать детальную информацию о комнатах, в том числе по наличию бронирований (1 балл).

4. Реализовать GET-форму на главной странице для фильтрации комнат по нажатию кнопки "Применить". Обрабатывать ... (2 балла)

5. Реализовать POST-форму на странице добавления бронирования (2 балла)

## (КТ8 + КТ9) Типизация работы с БД. Перенос на Express

_Максимальный балл - 14._

### Требования и критерии оценивания

#### Перенос проекта на Express 5

1. Перенос роутинга на `Express 5`

- переписать конечные точки `booking/booking.router.ts` и подключить их в `main.ts` (1 балл)
- переписать конечные точки `booking/timeslot.router.ts` и подключить их в `main.ts` (1 балл)

2. Перенос контроллеров на `Express 5`

- переписать и типизировать входные параметры контроллеров `booking/booking.router.ts` (1 балл) и `timeslot/timeslot.router.ts` (1 балл)
- переписать валидацию входных параметров и логику выдачи ответов для контроллеров `booking/booking.router.ts` (1 балл) и `timeslot/timeslot.router.ts` (1 балл)

#### Перенос слоя данных на Kysely

- вывести (либо написать вручную) интерфейсы для таблиц `timeslots` и `areas` (1 балл)
- подключить интерфейс(ы) к `database/interface.ts` (1 балл)

#### Перенос сервисов на Kysely

- перенести `timeslot/timeslot.service.ts` на типобезопасные запросы через `kysely` (1 балл за базовый запрос + 1 балл за реализацию фильтрации по времени суток)

- перенести `booking/booking.service.ts` на типобезопасные запросы через `kysely` (по 1 баллу за создание, удаление, получение одного бронирования, до 2 баллов за список бронирований с пагинацией и фильтрами)

- (\*) дописать `area/area.service.ts` согласно комментариям внутри (1 балл)

#### Доработка функционала по юзкейсам

Реализовать с использованием `kysely` сервис `area/area.service.ts::findOne`, соответствующий статический метод контроллера с использованием `express` в `area/area.controller.ts` и роут в `area/area.router.ts`, который должен выдавать полную информацию о помещении (либо ответ `404` при отсутствии переданного `id`). (до 2 баллов)

## (КТ6 + КТ7) Работа с API бронирований.

_Максимальный балл - 16._

### Требования и критерии оценивания

#### 1. Подсистема "Доступных помещений"

###### Назначение

Подсистема предназначена для получения списка доступных помещений.

- получение массива данных (идентификаторов и именований) о доступных помещениях, а также общего количества помещений;
- возможность поиска (фильтрации по именованиям);
- возможность пагинации;

###### Критерии приемки (7 баллов)

**main.ts**

- [х] суброутер из `area.router.ts` подключен к основному роутеру; _(0.5 балла)_

**/lib/schema.ts**

- [x] название ресурса внесено в схему ресурсов; _(0.5 балла)_

**/area/area.schema.ts**

- [ ] схема ответа соответствует требованиям к подсистеме; _(1 балл)_

**/area/area.router.ts**

- [x] реализована связка `GET /areas` с соответствующим методом контроллера `AreaController`; _(0.5 балла)_

**/area/area.controller.ts**

- [ ] статический метод `findAll` принимает на вход параметры запроса, передает их в соответствующий сервис _(0.5 балла)_;

- [ ] так как валидация параметров запроса производится на уровне роутера, а метод отдает массив помещений (который в случае отсутствия данных просто будет пустым), статус ответа всегда 200, а схема данных всегда едина. _(0.5 балла)_

**/area/area.service.ts**

- [ ] статический метод `findAll` принимает на вход параметры поиска и пагинации _(0.5 балла)_;

- [ ] при наличии параметра поиска формируется участок селект-запроса с `WHERE`; _(1 балл)_

- [ ] при наличии параметов пагинации формируется участок селект-запроса с `LIMIT` и `OFFSET`; _(1 балл)_

- [ ] возвращается результат на основе итогового селекта, содержащего описанные условные участки (пустой массив в случае отсутствия данных); _(1 балл)_

#### 2. Подсистема "Временных слотов"

###### Назначение

Подсистема предназначена для получения списка доступных помещений.

- получение массива данных о слотах (идентификаторы, время начала и конца слотов);

- возможность фильтрации слотов по времени суток (AM / PM);

###### Критерии приемки (7 баллов)

**main.ts**

- [ ] суброутер из `timeslot.router.ts` подключен к основному роутеру; _(0.5 балла)_

**/lib/schema.ts**

- [ ] название ресурса внесено в схему ресурсов; _(0.5 балла)_

**/timeslot/timeslot.schema.ts**

- [ ] схема ответа соответствует требованиям к подсистеме; _(1 балл)_

- [ ] на основе схем выведены типы; _(0.5 балла)_

**/timeslot/timeslot.router.ts**

- [x] реализована связка `GET /timeslots` с соответствующим методом контроллера `TimeslotController`; _(0.5 балла)_

**/timeslot/timeslot.controller.ts**

- [ ] статический метод `findAll` принимает на вход параметры запроса, передает их в соответствующий сервис; _(0.5 балла)_

- [ ] так как валидация параметров запроса производится на уровне роутера, а метод всегда отдает массив слотов, статус ответа всегда 200, а схема данных всегда едина. _(0.5 балла)_

**/timeslot/timeslot.model.ts**

- [ ] реализован геттер `AM`, возвращающий истину если слот завершается до полудня включительно и геттер `PM`, возвращающий истину если слот начинается после полудня включительно; _(1 балл)_

**/timeslot/timeslot.service.ts**

- [ ] статический метод `findAll` принимает на вход параметры фильтрации по времени суток; _(0.5 балла)_;

- [ ] независимо от наличия фильтрации селект-запрос получает все существующие временные слоты, которые далее трансформируются в экземпляры модели; _(0.5 балла)_;

- [ ] при необходимости производится фильтрация массива экземпляров моделей по предикату, через соответствующий геттер; _(0.5 балла)_

- [ ] возвращается массив результатов в соответствии с требованиями; _(0.5 балла)_

#### 3. Подсистема `bookings`

###### Назначение

Подсистема обеспечивает CRUD по бронированиям .

- создание бронирования;
- удаление бронирования;
- получение массива бронирований с краткой информацией о них и количеством полученных записей, возможность фильтра по помещениям и слотам, возможность пагинации;
- получение одного бронирования по идентификатору (с развернутой информацией о нем).

###### Критерии приемки (2 балла)

- внести изменения / дополнения в схемы данных (1 балл)
- перевести все существующие модули на TS (1 балл)

Полноценная имплементация подсистемы будет частью следующей контрольной точки.

---

## (КТ2 + КТ3) Работа с node:sqlite3. Создание cli-утилиты.

### Задачи и критерии оценивания (12 баллов + бонус)

#### Задача 1. Рефакторинг структуры проекта

✔️ 1.1. Создать директорию `src/database/cli` с модулями `index.js`, `seed.js`, `ddl.js`.

✔️ 1.2. Перенести в `seed.js` функцию `seedTables` и сделать ее экспортируемой по умолчанию.

✔️ 1.3. Перенести в `ddl.js` функции, обеспечивающие создание и сброс таблиц, использовать для них именованный экспорт.

✔️ 1.4. В `index.js` должна располагаться экспортируемая по умолчанию функция `cli`, вызывающая импортированные функции, описанные выше.

✔️ 1.5. (3 балла) Структура соответствует описанной выше, npm-скрипты `db:create`, `db:reset` и `db:seed` работают корректно.

#### Задача 2. Доработка функционала сидирования базы данных

✔️ 2.1. Выделить существующую логику наполнения таблицы `users` в функцию `seedUsers`, вызывать ее из `seedTables`.

✔️ 2.2. (2 балла) Создать функцию `seedBookings`, отвечающую за наполнение таблицы `bookings` двадцатью искусственными (но корректными!) записями, связанными по внешнему ключу с созданными ранее искусственными пользователями, вызывать ее из `seedTables`.

✔️ 2.3. (1 балл) Если таблицы не пустые, новые записи не добавлять, сообщать об этом в формате `таблица <название> уже содержит записи`.

✔️ 2.4. (1 балл) Добавить возможность уточнения списка сидируемых таблиц при запуске команд, например `npm run db:seed users` будет наполнять только таблицу пользователей, `npm run db:seed users bookings` будет наполнять обе. При запуске без дополнительных аргументов, по умолчанию наполнять все существующие таблицы.

#### Задача 3. Доработка ddl-функционала

✔️ 3.1 (1 балл) Добавить в `resetTables` логику для удаления всех записей из таблицы `bookings`.

✔️ 3.2 (1 балл) Добавить в `resetTables` возможность уточнения списка сбрасываемых таблиц по аналогии с 2.4.

✔️ 3.3 (1 балл) Добавить в `createTables` возможность форсированного создания таблиц (с удалением существующих, если они присутствуют), для этого предусмотреть флаги `--force` и `-F` при запуске через npm-скрипт.

#### Задача 4 (базовая). Доработка DX при использовании cli-утилиты

✔️ 4.1 (1 балл) Добавить help-сообщение, доступное через npm-скрипт `npm run db:help`.

✔️ 4.2 (1 балл) Используйте библиотеку [chalk](https://github.com/chalk/chalk) для красочного и форматированного вывода консольных сообщений и ошибок.

✔️ 4.3 (1 балл) Используйте на выбор [ora](https://github.com/sindresorhus/ora) либо [yocto-spinner](https://github.com/sindresorhus/yocto-spinner) для отображения спиннера во время выполнения действия.

#### Задача 4 (альтернатива). Доработка DX при использовании cli-утилиты

(до 5 баллов) Реализовать интерактивное взаимодействие с пользователем, используя на выбор:

- [inquirer.js](https://github.com/SBoudrias/Inquirer.js),
- [meow](https://github.com/sindresorhus/meow)
- [yargs](https://github.com/yargs/yargs)

#### Штрафные санкции

- (минус 0.5 балла) присутствует не отформатированный код,
- (минус 0.5 балла) присутствуют ошибки линтера,
- (минус 1 балл) работа сдана архивом

_Округление будет происходит по правилам математики (при этом половина будет округляться вверх)_

## Установка и настройка

1. Клонируйте репозиторий через `git clone https://github.com/ITHUB-SPB/ithub-node-project-1.git`
2. Откройте директорию в редакторе кода
3. Установите зависимости через `npm i`
4. Запустите сервер командой `npm run dev`.
5. Команда для форматирования `npm run format`, команда для проверки линтером `npm run lint`.
